[package]
name = "roar-ebpf-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["rmp-serde", "serde/std"]

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
rmp-serde = { version = "1", optional = true }
