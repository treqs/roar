[package]
name = "tracer-schema"
version = "0.1.0"
edition = "2021"
description = "Shared tracer report schema used by ptrace and eBPF backends"

[dependencies]
serde = { version = "1", features = ["derive"] }
