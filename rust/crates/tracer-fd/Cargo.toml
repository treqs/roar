[package]
name = "tracer-fd"
version = "0.1.0"
edition = "2021"
description = "Shared fd/path aggregation logic for roar tracers"

[dependencies]
tracer-schema = { path = "../tracer-schema" }

[lints.clippy]
all = { level = "warn", priority = -1 }
unwrap_used = "warn"
expect_used = "allow"
too_many_arguments = "allow"
